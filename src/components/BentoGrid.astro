---
import { cn } from "../lib/utils";

export interface BentoGridItemProps {
	className?: string;
}

const { className } = Astro.props as BentoGridItemProps;
---
<div
  class={cn(
    "grid md:auto-rows-[18rem] grid-cols-1 md:grid-cols-3 gap-4 max-w-7xl mx-auto",
    className
  )}
>
  <slot />
</div>
